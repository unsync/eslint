{
  "name": "@unsync/eslint",
  "type": "module",
  "version": "1.0.12",
  "description": "Shared eslint config for my projects",
  "license": "MIT",
  "repository": "unsync/eslint",
  "keywords": [
    "eslint-config"
  ],
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "test": "exit 0",
    "push": "npm version patch && git push",
    "push-minor": "npm version minor && git push",
    "push-major": "npm version major && git push"
  },
  "dependencies": {
    "@antfu/eslint-config": "^2.6.3"
  }
}
